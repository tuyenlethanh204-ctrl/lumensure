# Submission Bundle

This directory is intentionally metadata-only. The source repository and `evidence/`
directory are the submission artifacts.

Checked on 2026-07-03:

- no `.env`, secret key, seed phrase, funded identity, private witness, or credential is included;
- local and testnet evidence contain only public inputs, hashes, contract state, and transaction identifiers;
- reproducible commands are documented in the root README and `TESTNET_DEPLOYMENT.md`.
